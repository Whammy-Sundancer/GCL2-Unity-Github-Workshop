using UnityEngine;

public class PrintJohnson : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print(
            "Hello! My name is Johnson Bellbottom!" 
            );
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
