using UnityEngine;

public class Kosuzu_Scripts : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello I'm Kosuzu");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
